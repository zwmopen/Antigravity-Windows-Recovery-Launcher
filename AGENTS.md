# AI 接手规则

1. 先读 `README.md`、`docs/DESIGN.md`、`docs/ARCHITECTURE.md` 和 `docs/HANDOFF.md`。
2. `src/` 是源码真源；不创建平行源码副本。
3. `docs/HANDOFF.md` 是唯一权威开发交接文档；新状态就地更新，不另建“补充交接”。
4. 功能变化同步更新 `VERSION`、`README.md`、`CHANGELOG.md` 和受影响文档。
5. `.work/`、`build/`、`dist/` 只放可重建中间产物，不得放唯一原件或用户数据。
6. 发布前执行与风险匹配的测试，记录验收结果和已知限制。
7. 不将密码、Token、Cookie、验证码、手机号、用户作品和未脱敏日志写入仓库。
8. 删除、覆盖、数据迁移和发布前确认精确目标和恢复路径。
9. 所有功能都要能解释如何服务 README 标明的对象；与服务对象无关的功能默认不增加。
10. 严格执行自证式交付与省力化契约（遵循 `开发项目默认交付标准.md` §1.18）：严禁把用户当第一线测试员，交付前必须在后台完成端到端破坏性自测并出示机器证据链；用户只需陈述业务体感，技术推演、日志逆向与自测验证由 AI 全包。
11. 严格遵循既定黄金流水线：【先切号（物理直写+Cockpit同步）$\rightarrow$ 订阅更新 $\rightarrow$ 退出旧实例 $\rightarrow$ 启动器拉起专线 $\rightarrow$ 前 3 窗口扣 1 续接】与分级通知规范（5%仅桌面，成功发桌面+飞书，底层 90s 防抖熔断）。

---

## 📝 变更记录

| 日期 (时间) | 执行者 | 记录 |
|---|---|---|
| 2026-09-17 23:11 | 💻 本地 PC / 反重力 | 初始化创建文档并补齐变更记录历史 |
